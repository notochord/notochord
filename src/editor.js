/*
 * Code for the editor, but nothing specific to running it on the webpage.
 */

/* MusicEditor object, which houses everything else
 * @class
 */
function MusicEditor() {
  this.container = document.createElement('div');
}
